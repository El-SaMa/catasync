
# catasync-worker

Private Node.js worker for CataSync offload/enrichment. Not public.

Install: curl -L https://raw.githubusercontent.com/El-SaMa/catasync/main/install.sh | bash
Start:  cd /opt/catasync-worker && npm start
Service: systemctl enable --now catasync-worker
